// to use the watch mode - tsc -w {filename} or tsc --watch {filename}

interface Chiken {
    breed: string;
    eggsPerWeek: number;
    name: string;
    age: number;
}

const norma: Chiken = {
    breed: 'Silkie',
    eggsPerWeek: 4,
    name: 'Norma',
    age: 1,
};
